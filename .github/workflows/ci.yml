name: 'CI/CD Pipeline'

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened ]

permissions:
  contents: write
  pull-requests: write
  actions: read

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10
  RUST_BACKTRACE: short
  RUSTUP_MAX_RETRIES: 10

jobs:
  # ================================================================
  # STAGE 1: ENVIRONMENT SETUP
  # ================================================================
  setup:
    name: 'Setup Environment'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      should-release: ${{ steps.check-release.outputs.should-release }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if should release
        id: check-release
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" && "${{ github.base_ref }}" == "main" && "${{ github.head_ref }}" == "develop" ]]; then
            echo "should-release=true" >> $GITHUB_OUTPUT
          else
            echo "should-release=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup Rust environment
        uses: ./.github/actions/setup-rust
        with:
          components: 'rustfmt,clippy'
          install-cargo-audit: 'true'
          install-git-cliff: 'true'
          cache-key-suffix: 'setup'

      - name: Pre-fetch dependencies
        run: cargo fetch

  # ================================================================
  # STAGE 2: BUILD VERIFICATION
  # ================================================================
  build:
    name: 'Build Verification'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [setup]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-suffix: 'shared'

      - name: Build release
        run: cargo build --release

      - name: Build tests
        run: cargo build --tests

      - name: Find and upload build artifact
        shell: bash
        run: |
          # Find the binary (prefer clean name first)
          for binary_name in "disk-cleaner" "disk-cleaner-rs"; do
            if [ -f "target/release/${binary_name}" ]; then
              BINARY_PATH="target/release/${binary_name}"
              echo "Found binary: $BINARY_PATH"
              break
            fi
          done
          
          if [ -z "$BINARY_PATH" ]; then
            echo "❌ No binary found in target/release/"
            ls -la target/release/
            exit 1
          fi
          
          # Create artifact directory
          mkdir -p build-artifact
          cp "$BINARY_PATH" build-artifact/
          echo "BINARY_PATH=$BINARY_PATH" >> $GITHUB_ENV

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-verification
          path: build-artifact/

      - name: Smoke test binary
        run: |
          echo "🧪 Running smoke tests on compiled binary..."
          
          # Find the binary
          for binary_name in "disk-cleaner" "disk-cleaner-rs"; do
            if [ -f "target/release/${binary_name}" ]; then
              BINARY_PATH="target/release/${binary_name}"
              echo "Testing binary: $BINARY_PATH"
              break
            fi
          done
          
          # Test basic functionality
          echo "Testing --version flag..."
          $BINARY_PATH --version
          
          echo "Testing --help flag..."  
          $BINARY_PATH --help | head -5
          
          echo "✅ Smoke tests passed!"

  # ================================================================
  # STAGE 3: PARALLEL TESTING
  # ================================================================
  test-unit:
    name: 'Unit Tests'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-suffix: 'shared'

      - name: Run unit tests
        run: cargo test --bins --verbose

  test-integration:
    name: 'Integration Tests'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-suffix: 'shared'

      - name: Run integration tests
        run: cargo test --test integration_tests --verbose

  test-lint:
    name: 'Code Quality & Linting'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          components: 'rustfmt,clippy'
          cache-key-suffix: 'shared'

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Generate documentation
        run: cargo doc --no-deps --document-private-items

  test-security:
    name: 'Security Audit'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-suffix: 'shared'

      - name: Run security audit
        run: |
          # Ensure cargo-audit is available (fallback)
          if ! command -v cargo-audit &> /dev/null; then
            echo "🔧 cargo-audit not found, installing as fallback..."
            cargo install cargo-audit --locked
            echo "✅ cargo-audit installed successfully"
          fi
          
          # Verify cargo-audit is working
          cargo audit --version
          echo "🔍 Running security audit..."
          cargo audit

  # ================================================================
  # STAGE 4: VERSIONING (PR develop -> main only)
  # ================================================================
  auto-version:
    name: 'Auto Version & Tag'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [setup, build, test-unit, test-integration, test-lint, test-security]
    if: needs.setup.outputs.should-release == 'true'
    outputs:
      new-version: ${{ steps.version.outputs.new-version }}
      version-created: ${{ steps.version.outputs.version-created }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Auto version and tag
        id: version
        uses: ./.github/actions/auto-version
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  # ================================================================
  # STAGE 5: BASIC ARTIFACT CREATION (Simplified)
  # ================================================================
  build-artifacts:
    name: 'Build Artifacts'
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    needs: [auto-version]
    if: needs.auto-version.outputs.version-created == 'true'
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            platform: "linux-x64"
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            platform: "linux-musl"
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            platform: "windows-x64"
          - os: macos-latest
            target: x86_64-apple-darwin
            platform: "macos-x64"
          - os: macos-latest
            target: aarch64-apple-darwin
            platform: "macos-arm64"
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: "v${{ needs.auto-version.outputs.new-version }}"

      - name: Build artifacts
        uses: ./.github/actions/build-artifacts
        with:
          version: ${{ needs.auto-version.outputs.new-version }}
          target: ${{ matrix.target }}
          platform: ${{ matrix.platform }}

  generate-changelog:
    name: 'Generate Changelog'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [auto-version]
    if: needs.auto-version.outputs.version-created == 'true'
    outputs:
      changelog: ${{ steps.changelog.outputs.changelog }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: "v${{ needs.auto-version.outputs.new-version }}"

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          install-git-cliff: 'true'
          cache-key-suffix: 'changelog'

      - name: Generate changelog
        id: changelog
        run: |
          # Ensure git-cliff is available (fallback)
          if ! command -v git-cliff &> /dev/null; then
            echo "🔧 git-cliff not found, installing as fallback..."
            cargo install git-cliff --locked
            echo "✅ git-cliff installed successfully"
          fi
          
          # Verify git-cliff is working
          git-cliff --version
          echo "📝 Generating changelog..."
          
          CHANGELOG=$(git cliff --latest --strip header)
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          # Update CHANGELOG.md
          git cliff --output CHANGELOG.md

      - name: Upload changelog
        uses: actions/upload-artifact@v4
        with:
          name: changelog
          path: CHANGELOG.md

  # ================================================================
  # STAGE 6: RELEASE CREATION
  # ================================================================
  create-github-release:
    name: 'Create GitHub Release'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [auto-version, build-artifacts, generate-changelog]
    if: needs.auto-version.outputs.version-created == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: "v${{ needs.auto-version.outputs.new-version }}"

      - name: Create GitHub Release
        uses: ./.github/actions/github-release
        with:
          version: ${{ needs.auto-version.outputs.new-version }}
          changelog: ${{ needs.generate-changelog.outputs.changelog }}
          token: ${{ secrets.GITHUB_TOKEN }}